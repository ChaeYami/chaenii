# 백엔드 API base URL (필수)
NEXT_PUBLIC_API_URL=http://localhost:8080

# 점검 UI 강제 노출 (로컬 테스트용)
# "true"로 설정 시 헬스체크 없이 즉시 <MaintenanceView /> 렌더
# admin(/admin/*) 경로는 예외 — 항상 정상 렌더
# .env.local 에만 설정하고 커밋하지 말 것 (.gitignore에 이미 포함)
NEXT_PUBLIC_FORCE_MAINTENANCE=false
